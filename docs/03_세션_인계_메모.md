# 세션 인계 메모

> 작성: 2026-09-19 · 대상: 이 저장소를 이어받는 다음 세션
>
> 설계서(`01`)·계획서(`02`)·`README.md` 에 있는 내용은 반복하지 않는다.
> **대화에서만 결정됐고 코드나 문서에 안 남은 것들**을 적는다.

---

## 1. 지금 어디까지 왔나

**Phase 0~6 완료. Phase 7(실사용 1주) 진행 중 — 사용자가 직접 쓰는 단계라 대기.**

- 브랜치 `claude/read-this-uinay7`, 마지막 커밋 `c802ef3`
- 테스트 **87개 통과**, ruff clean
- 서울 전역 데이터 적재 완료 (노선 718 / 정류장 12,897 / 경유 41,688)
- **아이폰에서 실제로 동작 확인됨** — 홈 화면 아이콘, 위치 권한, 결과 5장까지

남은 확인 하나: **야외 GPS 정확도 실측**. 밖에서 앱을 열었을 때 상단이
"현재 위치 확인됨"인지 "오차 N m — 정확도가 낮아요"인지. 따로 할 작업은
없고 사용자가 보고 알려주기로 했다.

---

## 2. 사용자가 잡아준 것 — 같은 실수 반복하지 말 것

이 대화에서 **내가 두 번 틀렸고 두 번 다 사용자가 잡았다.** 둘 다 "그럴듯한
추론"이 실제 데이터와 달랐던 경우다. 새 세션도 같은 함정에 빠지기 쉽다.

### 2-1. `direction` 조인 (내가 넣었다가 되돌림)

`route_stop.direction` 이 행선지 종점명이라는 걸 알고, "회차 지점을 넘는
조합은 종점까지 갔다 와야 한다"고 **잘못 추론해** §9.1 질의에 조인을 걸었다.

사용자 지적: *"강남역9번출구에서 타면 진흥아파트, 서초푸르지오써밋,
신논현역 지나서 강남역1번출구 도착하는데, seq 66에서 70이면 4정거장 뒤인데
왜 상계동 종점에 가야 한다는 거지?"*

맞는 지적이었다. 데이터를 다시 보니 회차점은 종점이 아니라 **노선 한복판**
(기점에서 가장 먼 seq 67, 18.4km)이고 seq 135는 기점으로 돌아온다. 차량은
seq 1→N 을 연속으로 달리고 `direction` 은 앞 행선판이 바뀌는 것뿐이다.

→ 조인 제거. 조건은 `rs2.seq > rs1.seq` 하나.
→ `test_turnaround_point_is_mid_route_not_the_terminal` 이 전제를 고정한다.
**이 테스트를 지우거나 direction 조인을 되살리지 말 것.**

### 2-2. 후보 상한을 조합 탐색 *전에* 적용

`MAX_ORIGIN_STOPS=8` 을 거리순으로 먼저 잘랐다. 실데이터에서 드러났다:
강남역 반경 600m 정류장 39곳 중 시청행 직통이 있는 곳은 8곳뿐인데 거리순
상위 8곳과 거의 겹치지 않는다. 먼저 자르면 노선 9개 중 1개만 남았다.

→ 조합을 먼저 찾고 '직통이 있는 승차 정류장' 중에서 자른다.
→ 하차 쪽 상한(`max_dest_stops`)은 외부 호출이 없어 제거했다.
**config 에 다시 추가하지 말 것.**

### 교훈

픽스처 한 노선(146번)으로는 못 잡는 종류의 버그였다. **실데이터로 한 번
돌려보는 것**이 테스트보다 먼저 잡아낸다.

---

## 3. 실물 API가 뒤집은 가정

`README.md` 의 "API 실측 결과" 절에 정리돼 있다. 여기서는 **어떻게 알게
됐는지만** 적는다.

`scripts/smoke.py` 로 실제 응답을 받아 `tests/fixtures/` 에 저장한 것이
결정적이었다. 특히 `<stId>` 가 조회한 정류장을 가리키지 않는 건(경기
광역버스가 자기 기점 ID를 달고 온다) 픽스처 없이는 못 잡았을 버그다.
에러가 안 나고 "가끔 이상한 추천"으로만 나타난다.

**스펙이 의심되면 픽스처를 먼저 갱신하고 코드를 고쳐라.** 추측으로 파싱을
고치면 안 된다.

---

## 4. 환경 — 이걸 모르면 헤맨다

### 개발 컨테이너 (Claude Code 가 도는 곳)

**서울시 버스 API 가 조직 이그레스 정책에 403으로 막혀 있다.**
`ws.bus.go.kr`, `apis.data.go.kr`, `data.go.kr`, `data.seoul.go.kr` 전부.

- `scripts/smoke.py`, `nowbus plan` 은 **여기서 실행 불가**. 로컬에서만 된다.
- 테스트 87개는 전부 오프라인(픽스처 기반)이라 여기서 돈다.
- 프론트 확인은 목 Provider + Chromium 으로 했다.
  Playwright 는 pip 버전이 달라 `executable_path="/opt/pw-browsers/chromium-1194/chrome-linux/chrome"`
  를 줘야 한다. `playwright install` 은 실행하지 말 것.

### 사용자 로컬

**macOS (zsh).** 세션 초반엔 Windows(cmd)였다가 중간에 맥으로 옮겼다.
Windows 전용 명령(`winget` 등)을 주지 말 것.

배포는 **D-9 = A안(집 PC + 터널)** 으로 확정. 다만 Cloudflare Tunnel 이
아니라 **Tailscale Funnel** 을 쓴다:

- `trycloudflare.com` URL 은 재시작마다 바뀌어서 홈 화면 아이콘이 죽는다.
  "아침에 아이콘 한 번 탭"이라는 MVP 완료 정의와 맞지 않는다.
- Tailscale Funnel 은 도메인 없이 `https://<맥>.<tailnet>.ts.net` 고정 주소를
  무료로 준다. `brew install --cask tailscale-app` (CLI 전용
  `brew install tailscale` 은 데몬이 root 를 요구해서 막힌다).

### 매일 아침 돌아가야 하는 것

```zsh
tailscale funnel --bg 8000     # 한 번만. 재부팅해도 유지
caffeinate -s nowbus serve     # 이건 계속 떠 있어야 함
```

- 화면 잠김·디스플레이 꺼짐은 괜찮다. **시스템 잠자기**가 서버를 죽인다.
- `caffeinate -s` 는 전원 연결 시에만 유효하고 **뚜껑 닫기는 못 막는다.**
- launchd 자동 실행은 만들자고 제안했으나 사용자가 "일단 써보고" 로 보류.

---

## 5. 대화에서 결정된 것 (문서에 안 적힌 것)

| 항목 | 결정 | 이유 |
|---|---|---|
| 정적 데이터 수집 | **xlsx 다운로드** (OA-15067), API 호출 0회 | 노선 718개면 `getStaionByRoute` 만으로 일일 한도 1,000건 초과 |
| 노선 목록 확보 | 불필요해짐 | xlsx 에 이미 전 노선 경유 순서가 있다 |
| 병렬 조회 | `gather(return_exceptions=True)` | 계획서는 TaskGroup 을 제안했으나 형제 태스크를 취소해 부분 실패 허용이 깨진다 |
| 다양성 예외 | 개수 제한 없음 | `max_per_stop` = '비슷할 때 섞어라', `min_distinct_stops` = '항상 대안은 남겨라'. 상한으로 대안을 보장하려 들면 더 빠른 버스를 숨긴다 |
| FastAPI 의존성 | 전부 async | 동기 의존성은 스레드풀에서 돌아 sqlite3 연결이 스레드를 넘는다. `check_same_thread=False` 로 덮지 않았다 |
| 아이콘 | 코드로 생성 (`scripts/make_icons.py`) | 디자인 툴 없이 재현 가능하게 |
| service worker | 앱 셸까지 network-first | 계획서 §10-8 "캐시가 개발을 방해한다" 정면 대응. `?nosw=1` 탈출구 있음 |

---

## 6. 개인정보 — 저장소에 넣지 말 것

- **집·회사 좌표는 대화에만 있고 저장소에는 없다.** `place` 테이블은
  `data/*.db` 라 gitignore 대상이다. 문서·테스트·커밋에 실제 좌표를 쓰지 말 것.
- README 예시 좌표(`37.6602,127.0637` 등)는 전부 **임의의 랜드마크**다
  (상계동 7단지영업소, 강남역, 시청, 여의도, 홍대). 사용자 집이 아니다.
- 커밋 author 이메일은 `doubt4230@naver.com` 을 쓴다 (사용자 지정).
  Claude 커밋은 `noreply@anthropic.com`.
- `.env` 는 절대 커밋하지 않는다. API 키를 채팅에 붙여넣게 하지도 말 것 —
  `scripts/smoke.py` 는 키를 출력하지 않고 `getpass` 로 받는다.

---

## 7. 다음에 할 일

### 사용자 응답 대기 중

일주일 실사용 후 보고받기로 한 것:

- 도보 예상 vs 실제 (→ `WALK_DETOUR` 1.30, `CALIB`)
- 여유 2분에 실제로 탔는지 (→ `M_SAFE`)
- 맥북이 아침에 살아 있었는지 (→ **D-9 최종 판단**)
- 5개가 한 정류장에 몰리는지 (→ **D-10**)
- 네이버보다 나은 선택을 준 사례 / 이상한 추천을 한 사례

> 이미 한 번 "네이버가 안 알려준 정류장"이 나왔다. 사용자는 "오래 걸려서
> 안 알려준 것 같지만 넘어가자"고 했다. MVP 완료 정의의 절반은 충족됐다.

### 만들기로 했으나 보류된 것

사용자가 *"일단 며칠 써보고 실제로 불편한 게 뭔지 확인한 뒤"* 로 미뤘다.
**짐작으로 먼저 만들지 말 것.**

1. **F-11 실측 피드백 UI** — `POST /api/feedback` 은 있지만 폰에서 기록할
   화면이 없다. 그리고 `trip_log` 를 읽어 `CALIB` 을 갱신하는 코드도 없다.
   즉 지금 상태로는 도보 보정계수 학습이 굴러가지 않는다.
   설계서 §12가 도보 오차를 **핵심 리스크**로 지목했으므로 Phase 7 튜닝이
   주관에 의존하게 된다. 사용자에게 이 점은 이미 알렸다.
2. `nowbus calib` — `trip_log` 기반 보정계수 제안 (자동 적용은 안 함)
3. 즐겨찾기 **삭제** (등록만 있음)
4. 목적지 버튼 길게 눌러 편집
5. launchd 자동 기동

### v2 이후 (설계서 §14)

1회 환승 · 출발 역산 알림(웹 푸시) · 정시성 통계 · 개인화 학습 · 날씨 연동

---

## 8. 작업 스타일

사용자 선호(설정에 등록돼 있음):

- **한국어**, 주로 Python
- **설명은 핵심만 확실히.** 장황한 나열보다 결론과 근거
- **모르는 건 모른다고.** 웹 검색으로도 확인 안 되면 추측하지 말고 그렇게 말할 것

이 대화에서 실제로 잘 작동한 방식:

- 데이터 모양이 의심되면 **추측하지 말고 실물을 받아서 확인**한다
- 사용자가 반론하면 **먼저 데이터를 다시 본다.** 두 번 다 사용자가 옳았다
- 작업 단위마다 커밋하고, 커밋 메시지에 **왜 그렇게 했는지**를 남긴다
  (특히 계획서와 다르게 간 경우)
- 사용자가 로컬에서 할 일과 컨테이너에서 할 일을 명확히 나눠서 안내한다
